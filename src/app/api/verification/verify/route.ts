/**
 * 공개 문서 검증 — `/api/document-verification` 과 동일 서비스.
 * Batch A 별칭 경로. 구현·`ok`/`toErrorResponse` 정렬은 `document-verification/route.ts` 를 본다.
 */
export { POST } from "../../document-verification/route";
