/**
 * 문서 초안 생성 — 본선 구현은 `documents/generate/route.ts` (templateCode·질문셋 버전 기준).
 * Batch A 별칭: POST 본문·동작은 generate 와 동일합니다.
 */
export { POST } from "./generate/route";
export const dynamic = "force-dynamic";
